#include <stdio.h>

int main(void)
{
	int T;
	
	scanf("%d", &n);
	while (T--)
	{
		int type, x;
			
		scanf("%d", &type);
		if (type == 1)
		{
			scanf("%d", &x);
		}
		else
		{
			
		}		
	
	}
	
	return 0;
}
